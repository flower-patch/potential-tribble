<!DOCTYPE html>
<html>
  <head>
    <link href='http://fonts.googleapis.com/css?family=Lato:300,400,700,400italic|Kreon:300,400' rel='stylesheet' type='text/css'>j
    <link rel="stylesheet" href="#">
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width">
    <title>Title</title>
  </head>
  <body>

    <h1>Quilts#edit_project</h1>
    <p>Find me in app/views/quilts/edit_project.html.erb</p>
    <%= form_for(@quilt) do |f| %>
      <% if @quilt.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@quilt.errors.count, "error") %> prohibited this quilt from being saved:</h2>

          <ul>
          <% @quilt.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

      <div>
        <%= f.label :svg %><br>
        <%= f.text_area :svg %>
      </div>
      <div >
        <%= f.submit "Save Project" %>
      </div>
    <% end %>

  </body>

</html>
