<h1>Quilts#next_steps</h1>
<!-- page 5 wireframe -->
<p>Find me in app/views/quilts/next_steps.html.erb</p>


<p>Full SVG</p>
<%#= @quilt.svg %>
<%= @quilt.get_paths_from_svg %>

<%# FIXME: this if statement is a hack to avoid the pain of ERB commenting %>
<% if false %>
<% Fabric.get_spoonflower_images( "goat" ).each do |hash| %>
  <p>Image ID:<%= hash[:id] %><p>
  <%= image_tag hash[:thumbnail] %>
  <br>
<% end %>
<% end %>


<%# FIXME: this if statement is a hack to avoid the pain of ERB commenting %>
<% if true %>
<% Fabric.get_spoonflower_fabrics_by_color( "FAC8BF" ).each do |hash| %>
  <p>Image ID:<%= hash[:id] %><p>
  <%= image_tag hash[:thumbnail] %>
  <br>
<% end %>
<% end %>
